import 'package:flutter/material.dart';

import '../../shared/widgets/widgets.dart';

/// Fase 4 · Dashboard de KPIs. Placeholder até a implementação dos gráficos.
class KpisScreen extends StatelessWidget {
  const KpisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const BrutEmpty(
      message: 'Dashboard de KPIs — em construção (fase 4).',
      icon: Icons.bar_chart_outlined,
    );
  }
}
